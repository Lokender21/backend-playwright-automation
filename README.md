# backend-playwright-automation
API automation framework using Playwright with Allure reporting and GitHub Actions integration. The framework validates asynchronous backend workflows including file upload, processing, polling, and artifact validation.
